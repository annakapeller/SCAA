def lambda_handler(event, context):
    response = {
        'statusCode': 200,
        'body': "I am just a dummy function to simulate the calculation module of the purifier plugin module. I don't do anything"
    }

    return response
